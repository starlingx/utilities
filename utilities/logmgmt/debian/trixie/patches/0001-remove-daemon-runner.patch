Description: Remove daemon.runner dependency for Trixie compatibility
 The daemon.runner module was removed from python3-daemon in Trixie.
 This patch removes the daemon dependency and simplifies the code to
 run directly without the DaemonRunner wrapper.

--- a/logmgmt/logmgmt.py
+++ b/logmgmt/logmgmt.py
@@ -20,8 +20,6 @@ import subprocess
 import sys
 import time
 
-from daemon import runner
-
 from logmgmt import prepostrotate
 
 ###################
@@ -39,9 +37,7 @@ LOGROTATE_PERIOD = 600  # Every ten min
 ###################
 def start_polling():
     logmgmt_daemon = LogMgmtDaemon()
-    logmgmt_runner = MyDaemonRunner(logmgmt_daemon)
-    logmgmt_runner.daemon_context.umask = 0o022
-    logmgmt_runner.do_action()
+    logmgmt_daemon.run()
 
 
 def handle_exception(exc_type, exc_value, exc_traceback):
@@ -53,18 +49,6 @@ def handle_exception(exc_type, exc_valu
 ###################
 # CLASSES
 ###################
-class MyDaemonRunner(runner.DaemonRunner):
-    # Workaround: fix the "unbuffered bytes I/O for py3" runtime
-    # error in pyhon3 env.
-    def _open_streams_from_app_stream_paths(self, app):
-        self.daemon_context.stdin = open(app.stdin_path, 'rt')
-        self.daemon_context.stdout = open(app.stdout_path, 'w+t')
-        try:
-            self.daemon_context.stderr = open(app.stderr_path, 'w+t', buffering=0)
-        except:
-            self.daemon_context.stderr = open(app.stderr_path, 'wb+', buffering=0)
-
-
 class LogMgmtDaemon():
     """Daemon process representation of the /var/log monitoring program"""
     def __init__(self):
@@ -171,7 +155,7 @@ class LogMgmtDaemon():
             logging.error('Failed to determine unmonitored files')
 
     def purge_files(self, index):
-        pattern = re.compile('.*\.([0-9]*)\.gz')
+        pattern = re.compile(r'.*\.([0-9]*)\.gz')
         for fname in sorted(self.monitored_files):
             result = pattern.match(fname)
             if result:
